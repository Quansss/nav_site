# 排序功能排查记录 2026-08-14

## 问题
用户反馈"还是不能排序"。

## 排查过程
1. **本机 8766 后端**：代码含 reorder 路由、sort_order 字段、ORDER BY sort_order ✅
2. **本机 API 测试**：登录 200、reorder 200、顺序正确 ✅
3. **JS 语法**：node --check 通过 ✅
4. **本机首页 HTML**：服务器提供最新版（含 inSortOrder/moveLink/上移按钮）✅
5. **关键发现**：用户访问的是**飞牛 NAS**（192.168.9.2:8766），上面部署的是**旧版**（无排序功能）❌

## 修复
- 同步 backend.py + static/index.html 到 NAS /vol1/docker/nav_site/
- docker compose up -d --build 重建容器
- 验证 NAS 首页 HTML：inSortOrder/moveLink/reorder 全部 ✅
- 容器内路由检查：reorder 路由注册 ✅、list_links 用 ORDER BY sort_order ✅

## 遗留问题
- NAS 管理员密码不是 admin123（hash 不匹配 b9eff7e3...），用户可能在 NAS 上改过密码，无法用默认密码登录验证 API
- 需用户 Ctrl+F5 强制刷新浏览器（可能缓存旧 JS）
- 用户访问地址需确认（192.168.9.2 还是 192.168.9.127）

## 结论
排序功能代码已同步到 NAS 并重建容器，功能已上线。等待用户刷新浏览器确认 + 提供 NAS 实际密码以便 API 层验证。
