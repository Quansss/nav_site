# nav_site 飞牛 NAS Docker 部署记录

**日期**：2026-08-13 22:10 (GMT+8)

## 部署目标
- NAS：192.168.9.2（飞牛 fnOS），SSH：shark / Quan0526@（22端口）
- 架构：x86_64，Docker 28.5.2 + Compose v2.40.3
- 部署目录：`/vol1/docker/nav_site`
- 访问地址：**http://192.168.9.2:8766**

## 部署结果 ✅
- 镜像：`nav_site-nav_site:latest`（基于 python:3.11-slim，125MB 基础 + 172MB 最终）
- 容器：`nav_site`，状态 Up，`0.0.0.0:8766->8766/tcp`
- 数据卷：`/vol1/docker/nav_site/data` → 容器 `/app/data`（nav.db 持久化）
- 默认管理员：`971954959@qq.com` / `admin123`（容器全新数据库自动创建）

## 验证结果
1. ✅ 首页 200，含"迅鲨云"
2. ✅ `/api/links` 返回 2 条示例链接（Google/百度，guest 可见）
3. ✅ 登录 → access_token（HMAC JWT，长度 200）
4. ✅ `/api/auth/me` 返回 admin 用户
5. ✅ 创建链接 → 可见 → 删除（id=6）全流程
6. ✅ 数据持久化：宿主 `/vol1/docker/nav_site/data/nav.db` 24KB

## 关键踩坑（复用以备后续）

### 1. 飞牛 Docker 镜像源 401
- `/etc/docker/daemon.json` 配置 `registry-mirrors: ["https://docker.fnnas.com", "https://registry.hub.docker.com"]`
- `docker.fnnas.com` 返回 401 Unauthorized（HEAD /v2/... 失败）
- **解法**：Docker 会自动 fallback 到官方源，直接 `docker pull python:3.11-slim` 成功（走了 hub.docker.com）
- 结论：构建时 buildkit 也会 fallback，无需改 daemon 配置

### 2. paramiko SFTP Windows 路径反斜杠陷阱
- `sftp.put(local, 'static\\index.html')` → NAS 上创建了**字面文件名** `static\index.html`（反斜杠是合法文件名字符！）
- 导致 Docker COPY 复制空 static/ 目录 → 容器内 `FileNotFoundError: 'static/index.html'` → 首页 500
- **解法**：远端路径必须用正斜杠 `static/index.html`
- 教训：paramiko SFTP 远端路径用 posix 风格，绝不传 Windows 路径

### 3. SSH 认证
- 旧 root/123456 已失效（360 项目时期密码）
- 当前：`shark` / `Quan0526@`，sudo 需要 `echo 密码 | sudo -S`
- shark 不在 docker 组，所有 docker 命令必须 sudo

### 4. 登录 API 字段名
- 登录返回 `{"access_token": "...", "token_type": "bearer", "user": {...}}`
- **不是** `token` 字段！测试脚本最初取错字段导致误判

## 容器管理命令（备忘）
```bash
# 在 NAS 上
cd /vol1/docker/nav_site
sudo docker compose logs -f nav_site   # 看日志
sudo docker compose restart nav_site    # 重启
sudo docker compose down && sudo docker compose up -d --build  # 更新部署
sudo docker inspect nav_site --format '{{json .Mounts}}'  # 查挂载
```

## 更新部署流程（改代码后）
1. 本地改代码
2. SFTP 上传（注意 static/ 正斜杠）
3. `cd /vol1/docker/nav_site && sudo docker compose up -d --build`
